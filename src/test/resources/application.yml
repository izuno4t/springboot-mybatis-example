logging:
    level:
        root: INFO
        sample:
            mybatis:
                mapper: TRACE
spring:
    datasource:
        driver-class-name: org.postgresql.Driver
        password: ${POSTGRES_PASSWORD:sample}
        url: jdbc:postgresql://localhost:5433/${POSTGRES_TEST_DB:sample-test}
        username: ${POSTGRES_USERNAME:sample}
    jpa:
        hibernate:
            ddl-auto: update
